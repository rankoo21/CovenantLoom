import {readFile} from "node:fs/promises";import {createAccount,createClient} from "genlayer-js";import {studionet} from "genlayer-js/chains";import {TransactionStatus} from "genlayer-js/types";
const key=process.env.GENLAYER_PRIVATE_KEY;if(!key)throw new Error("GENLAYER_PRIVATE_KEY is required");const account=createAccount(key.startsWith("0x")?key:`0x${key}`);const client=createClient({chain:studionet,account});const code=await readFile(new URL("../contracts/covenant_loom.py",import.meta.url),"utf8");const hash=await client.deployContract({code,args:[]});console.log("DEPLOY_TX="+hash);const receipt=await client.waitForTransactionReceipt({hash,status:TransactionStatus.FINALIZED,retries:180,interval:2500});console.log("CONTRACT_ADDRESS="+(receipt.to_address||receipt.recipient));
